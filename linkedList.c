#include <stdio.h>
#include <stdlib.h>
struct node
{
	int data;
	struct node* next;
};

void insert(int item){

	struct node *n{i}=(struct node*)malloc(sizeof(struct node));
	i++;
}

void main()
{
	struct node *n1=(struct node*)malloc(sizeof(struct node));
	struct node *n2=(struct node*)malloc(sizeof(struct node));
	n1->data=1;
	n1->next=n2;
	n2->data=2;
	n2->next=NULL;
	printf("%d\n", n1->data);

	int ch,item,i;

	while(1){
		printf("Enter your choice: ")
		scanf("%d",&ch);

		switch(ch){
			case 1:
				break;
		}
	}
}